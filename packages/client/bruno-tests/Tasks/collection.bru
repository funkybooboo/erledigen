meta {
  name: Tasks
  type: folder
}

docs {
  # Task API Tests

  This folder contains tests for task CRUD operations:
  - Create task
  - Get all tasks
  - Get task by ID
  - Update task
  - Delete task
  - Task with metadata (notes, color, tags, links, attachments)
}
